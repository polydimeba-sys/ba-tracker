# No external dependencies — server.py uses only the Python standard library.
# This file exists so hosting platforms (Railway, Render, etc.) detect this
# as a Python app and know which runtime to build with.
